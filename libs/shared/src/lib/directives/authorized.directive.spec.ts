import { AuthorizedDirective } from './authorized.directive';

describe('AuthorizedDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthorizedDirective();
    expect(directive).toBeTruthy();
  });
});
